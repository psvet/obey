/*
 * Copyright (c) 2015 TechnologyAdvice
 */

const rules = {
  /**
   * Adds new rule
   * @param {Object} def The rule definition
   * @returns {Object}
   */
  add: (def) => {
    return def
  }
}

export default rules
